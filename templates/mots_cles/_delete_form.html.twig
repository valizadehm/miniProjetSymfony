<form method="post" action="{{ path('app_mots_cles_delete', {'id': mots_cle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mots_cle.id) }}">
    <button class="btn">Delete</button>
</form>
